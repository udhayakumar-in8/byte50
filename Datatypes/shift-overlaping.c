#include<stdio.h>

int main(void)
{

    int i=132;
    /*he  << or Right operator (>>right value ) will be negative or overlapping than size
     the result will un behaviour other than light shift abiously unbheving number and right will be
      always given number as Result */
    printf("%d\n",i<<33);
}