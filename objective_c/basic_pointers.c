#include<stdio.h>
int main()
{
   /* int n, i;
   printf("Enter limit string\n");
   scanf("%d", &n);
   char *a[n];
 // the code segment is fixed size memory we cant assign the run time data
   printf("Enter the some string\n");
   char *p;
   for(i=0; i < n; i++) 
       scanf("%s",a[i]);

    printf("Result\n");
     for(i=0; i < n; i++)
       printf("%s",a[i]);
       */

      char *n[3] ,*p ;
      int i;
     char *P;
    for (i = 0; i < 3; i++)   
    {
     scanf("%s",p);
    // n[i] = p;
    }

  for(i = 0; i < 3; i++)     
   printf("%s",n[i]);

}